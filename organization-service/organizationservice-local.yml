server:
  port: 8000

spring:
  datasource:
    url: jdbc:postgresql://organizationdb:54320/organizationdb
    username: admin 
    password: admin
  
  jpa:
    properties: 
      hibernate:
        temp:
          use_jdbc_metadata_defaults: false
    database-platform: org.hibernate.dialect.PostgreSQL9Dialect
    
flyway:
  locations: db/migration
