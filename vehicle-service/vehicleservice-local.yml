server:
  port: 8000

spring:
  datasource:
    url: jdbc:postgresql://vehicledb:5432/vehicledb
    username: admin 
    password: admin
  
  jpa:
    properties: 
      hibernate:
        temp:
          use_jdbc_metadata_defaults: false
    database-platform: org.hibernate.dialect.PostgreSQL9Dialect
    
flyway:
  locations: db/migration
